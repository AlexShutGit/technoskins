import "./style.scss"

const Navbar = () => {
    return ( 
        <div class="navbar">
            <a href="#">Запись</a>
            <a href="#">Примеры работ</a>
            <a href="#">Прайс</a>
            <a href="#">Контакты</a>
        </div> 
    );
}

export default Navbar;